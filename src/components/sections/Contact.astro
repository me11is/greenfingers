<section id="contact">
    <h2>Get in touch</h2>
    <div class="contact">
        <div class="contact-socials">
            <h3>Socials</h3>
            <ul class="contact-socials-links">
                <li><a href="https://www.instagram.com/greenfingers_film/" target="_blank">Instagram</a></li>
                <li><a href="https://vimeo.com/user2817712" target="_blank">Vimeo</a></li>
                <li><a href="https://www.youtube.com/@greenfingersfilms5979" target="_blank">Youtube</a></li>
                <li><a href="https://www.tiktok.com/@greenfingersfilm?ug_source=op.auth&ug_term=Linktr.ee&utm_source=awyc6vc625ejxp86&utm_campaign=tt4d_profile_link&_r=1" target="_blank">Tiktok</a></li>
            </ul>
        </div>
        <div class="contact-email">
            <h3>Email</h3>
            <ul class="contact-socials-links">
                <li>
                    <button class="copy-button">
                        HELLO@GREENFINGERSFILM.COM
                        <p class="copy-text">Click to copy</p>
                        <p class="copy-text-placeholder">Click to copy</p>
                    </button>
                </li>
            </ul>
        </div>
    </div>
</section>

<style>
section {
    background-color: var(--color-mint);
    height: 100vh;
    width: 100vw;
    /* position: fixed; */
    /* bottom: 0; */
    z-index: -10;
    display: flex;
    flex-direction: column;
    padding-left: 1rem;
}

button {
    all: unset;
}

h2 {
    color: var(--color-dark-green);
    font-size: 7rem;
    margin-top: 9rem;
    margin-bottom: 1rem;
}

.contact {
 */
}

h4 {
    font-size: 1.2rem;
    opacity: .6;
}

a, button, button p {
    text-decoration: none;
    text-transform: uppercase;
    color: var(--color-mint);
    font-family: var(--font-humane);
    font-size: 3rem;
}

ul {
    list-style: none;
    margin: 0;
    padding: 0;
}

.copy-button {
    display: flex;
    position: relative;
}

.copy-button p {
    margin: 0;
}

.copy-text {
    position: absolute;
    right: 0;
    padding-left: .4rem;
    opacity: 0.6
}

.copy-text-placeholder {
    opacity: 0;
    padding-left: .4rem;
}

.contact-socials {
    display: flex;
    flex-direction: column;
    line-height: 1;
}

.contact-socials h3 {
    color: var(--color-dark-green);
    opacity: 0.6;
}

.contact-socials li a {
    color: var(--color-dark-green)
}

.contact-email {
    margin-top: 2rem;
    max-width: 90vw;
    background-color: var(--color-dark-green);
    -webkit-box-shadow: -4px 4px 0px 0px #0f0f0f;
    -moz-box-shadow: -4px 4px 0px 0px #0f0f0f;
    box-shadow: -4px 4px 0px 0px #0f0f0f;
}

.contact-email h3 {
    display: none;
}

.contact-email button {
    font-size: 2rem;
    padding: .4rem .8rem;
}

.contact-email p {
    font-size: 2rem;
    margin-left: 1rem;
}

@media (min-width: 768px) {
  /* tablet */
}

@media (min-width: 1024px) {
  /* laptops */
    h2 {
        font-size: 16rem;
        margin-top: 14rem;
        margin-bottom: 1rem;
    
    }

    section {
        padding-left: 2rem;
    }

    .contact {
        background-color: var(--color-dark-green);
        display: flex;
        justify-content: space-between;
        align-items: center;
        max-width: 80vw;
        padding: 3rem 2rem 2rem;
        -webkit-box-shadow: -8px 8px 0px 0px #0f0f0f;
        -moz-box-shadow: -8px 8px 0px 0px #0f0f0f;
        box-shadow: -8px 8px 0px 0px #0f0f0f;
    }

    .contact-socials, .contact-email {
        display: flex;
        flex-direction: column;
        align-items: flex-start;
    }

    .contact-email {
        box-shadow: none;
        margin: 0;
    }

    .contact-socials-links {
        display: flex;
        flex-direction: row;
        gap: 1rem;
    }

    .contact-socials-links li a, 
    .contact-socials-links li p,
    .contact-socials-links li button {
        color: var(--color-mint);
        font-size: 3rem;
    }

    .contact-socials h3,
    .contact-email h3 {
        color: var(--color-mint);
        opacity: .6;
        display: block;
        padding-bottom: .2rem;
    }

    .contact-email .copy-button {
        padding: 0;
        margin: 0;
        line-height: 1;
    }

}

@media (min-width: 1200px) {
 /* desktops */
}
</style>

<script>
      import JSConfetti from "js-confetti";

  const jsConfetti = new JSConfetti();

  const copyButton = document.querySelectorAll(".copy-button");
  const copyText = document.querySelectorAll(".copy-text");

  function copyEmail() {
    navigator.clipboard.writeText("hello@greenfingersfilm.com");
    copyText.forEach((item) => (item.textContent = "copied!"));
    jsConfetti.addConfetti({
      emojis: ["💌", "✉️"],
      confettiRadius: 4,
      confettiNumber: 70,
    });
    setTimeout(() => {
      copyText.forEach((item) => (item.textContent = "click to copy"));
    }, 2000);
  }

  copyButton.forEach((button) => button.addEventListener("click", copyEmail));
</script>